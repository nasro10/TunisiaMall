{% extends "base2.html.twig" %}
{% block body %}
<h2>Liste des Promotion</h2>
</div>
{% for i in Promotion %}
<style>
    label { font-family: 'Trocchi', serif; font-weight: normal; line-height: 48px; margin: 0; }
</style>


<div class="container">
    <div class="row">
        <div class="panel panel-default">
            <div class="panel-body">
                <div class="col-md-4 col-xs-12 col-sm-6 col-lg-4">

                </div>
                <div class="col-md-8 col-xs-12 col-sm-6 col-lg-8" >
                    <div class="container" >


    <ul class="container details">
    <li>Date Debut: {{ i.dateDeb|date('Y-m-d') }}</li>
    <li>Date Fin: {{ i.dateFin |date('Y-m-d')}}</li>
    <li>Pourcentage de Promotion: {{ i.prixPromo}}%</li>
        <tr><a href="{{ path('supprimePromotion',{'id':i.idPromo()}) }}"><button class="btn btn-outline rounded b-danger text-danger">Supprimer</button></a></tr>
        <tr><a href="{{ path('modificationPromotion',{'id':i.idPromo()}) }}"><button class="btn btn-outline rounded b-warning text-warning">Modifier</button></a></tr>
</ul>
                </div></div></div></div></div>
    <hr>
{% endfor %}
{% endblock %}
